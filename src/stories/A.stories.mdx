import { Story, Preview, Meta } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { html } from "lit-html";

import "../template-sources/custom/A.css";
import "../template-sources/custom/A";

# Storybook Docs for A

<Meta title="custom/A" />

## Story definition for A links

You can write mdx with the same file name in this `custom` folder and when you run `yarn build:stories` it will compile into your stroybook folder. Pretty cool aye.

In this case the three files that you can use to compile a full story would be:

- A.html
- A.css
- A.mdx

<Preview>
  <Story name="A" height="100px">
    {html` <a id="link" class="g-link example" href="example"> Example text</a> `}
  </Story>
</Preview>
